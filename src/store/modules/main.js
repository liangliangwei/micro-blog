
const state = {
    data: {
        
    }
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
    setText(state, data) {
        state.data = data;
    }
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
